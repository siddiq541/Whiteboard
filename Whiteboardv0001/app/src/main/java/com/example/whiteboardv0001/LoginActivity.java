package com.example.whiteboardv0001;

import android.app.Activity;
import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.EditText;

public class LoginActivity extends Activity {
/*
    public static final String EXTRA_MESSAGE = "com.example.scaledrone.chat.MESSAGE";
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_login);
    }

    public void signIn(View view)
    {
        // Grab the username from the screen, then start up a new messaging activity screen
        Intent intent = new Intent(this, MainActivity.class);
        EditText username_input = (EditText) findViewById(R.id.username_input);
        String username = username_input.getText().toString();
        intent.putExtra(EXTRA_MESSAGE, username);
        startActivity(intent);

        // The line below line starts new activity without sending any User info
        // startActivity(new Intent(this, MainActivity.class));

    }*/
}


